from django.contrib import admin

# The patient app has no models of its own.
# Patients and related models are registered in users/admin.py.
# This file is intentionally minimal — add patient-specific
# admin views here if the app grows (e.g. a PatientNote model).